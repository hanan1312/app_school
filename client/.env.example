# Optional. By default the client reaches the API at the page's own hostname on port 4000
# (see src/lib/api.ts). Only set these to override that, e.g. behind a reverse proxy.
# VITE_API_URL=https://school.example.com/api
# VITE_API_PORT=4000
